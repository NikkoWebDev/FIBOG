---
// Breadcrumb.astro - Breadcrumbs (new palette)

interface Props {
  items: Array<{ label: string; url?: string }>;
}

const { items } = Astro.props;
---

<nav aria-label="Breadcrumb" class="py-3 text-sm">
  <ol class="flex items-center gap-2 text-text-muted">
    <li>
      <a 
        href="/" 
        class="hover:text-primary-light transition-colors"
      >
        Inicio
      </a>
    </li>
    {items.map((item, index) => (
      <li class="flex items-center gap-2">
        <span class="text-border-default">/</span>
        {item.url ? (
          <a 
            href={item.url} 
            class="hover:text-primary-light transition-colors"
          >
            {item.label}
          </a>
        ) : (
          <span class="text-text-primary font-medium">{item.label}</span>
        )}
      </li>
    ))}
  </ol>
</nav>
