---
import type { Grupo } from '../data/grupos.types';

interface Props {
  grupo: Grupo;
  size?: 'small' | 'medium' | 'large' | 'wide';
  index?: number;
}

const { grupo, index = 0 } = Astro.props;

const tipoColors = {
  'Semillero': 'bg-tertiary/10 text-tertiary',
  'Grupo de Investigación': 'bg-primary/10 text-primary',
  'Grupo Estudiantil': 'bg-secondary/10 text-secondary'
};

const tipoBadge = tipoColors[grupo.tipo as keyof typeof tipoColors] || tipoColors['Semillero'];
---

<div class="bento-card card rounded-2xl p-6 border border-white/10 hover:border-primary/50 transition-all group" data-index={index}>

<div class="relative z-10">
    <!-- Header -->
    <div class="flex items-start justify-between mb-4">
      <div class="flex-1">
        <div class="flex items-center gap-2 mb-2">
          <span class={`px-2 py-1 rounded-md text-[10px] font-label-mono uppercase tracking-wider ${tipoBadge}`}>
            {grupo.tipo}
          </span>
        </div>
        <h3 class="text-lg font-display-lg text-white mb-1 group-hover:text-primary transition-colors line-clamp-2">
          {grupo.nombre}
        </h3>
        <p class="text-sm text-on-surface-variant line-clamp-2">{grupo.descripcion || 'Sin descripción'}</p>
      </div>
    </div>
    
    <!-- Info -->
    <div class="space-y-2 mb-4">
      {grupo.docente && (
        <div class="flex items-center gap-2 text-sm">
          <span class="material-symbols-outlined text-on-surface-variant text-base">person</span>
          <span class="text-on-surface-variant truncate">{grupo.docente}</span>
        </div>
      )}
      {grupo.carreras && grupo.carreras.length > 0 && (
        <div class="flex items-center gap-2 text-sm">
          <span class="material-symbols-outlined text-on-surface-variant text-base">school</span>
          <span class="text-on-surface-variant truncate">{grupo.carreras.slice(0, 2).join(', ')}</span>
        </div>
      )}
    </div>

    <!-- Actions -->
    <div class="flex items-center gap-2 pt-4 border-t border-white/10">
      <a 
        href={`/grupo/${grupo.id}`}
        class="flex-1 flex items-center justify-center gap-2 px-3 py-2 rounded-lg bg-surface-container hover:bg-white/10 transition-all text-sm text-center"
      >
        <span class="material-symbols-outlined text-base">visibility</span>
        <span>Ver detalles</span>
      </a>
      {grupo.email && (
        <a 
          href={`mailto:${grupo.email}`}
          class="p-2 rounded-lg bg-surface-container hover:bg-primary/20 text-primary transition-all"
          onclick="event.stopPropagation();"
        >
          <span class="material-symbols-outlined text-base">email</span>
        </a>
      )}
    </div>
  </div>
</div>
